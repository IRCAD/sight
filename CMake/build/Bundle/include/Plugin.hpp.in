#ifndef __@BUNDLE_NAME_UPPER@_PLUGIN_HPP__
#define __@BUNDLE_NAME_UPPER@_PLUGIN_HPP__

#include <fwRuntime/Plugin.hpp>

#include "@BUNDLE_NAME@/config.hpp"

namespace @BUNDLE_NAME@
{
/**
 * @brief   This class is started when the bundles is loaded.
 * @class   Plugin
 */
struct @BUNDLE_NAME_UPPER@_CLASS_API Plugin : public ::fwRuntime::Plugin
{
    /**
     * @brief   destructor
     */
    @BUNDLE_NAME_UPPER@_API ~Plugin() throw();

    // Overrides
    @BUNDLE_NAME_UPPER@_API void start() throw(::fwRuntime::RuntimeException);

    // Overrides
    @BUNDLE_NAME_UPPER@_API void stop() throw();

};

} // namespace @BUNDLE_NAME@

#endif //__@BUNDLE_NAME_UPPER@_PLUGIN_HPP__
